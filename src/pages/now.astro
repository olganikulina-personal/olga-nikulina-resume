---
import RouteLayout from "../layouts/RouteLayout.astro";
import { cards } from "../lib/cards";

const card = cards.find((c) => c.slug === "now")!;
---

<RouteLayout
  slug={card.slug}
  callNumber={card.callNumber}
  title={card.title}
  description="what's live, right now."
>
  <p class="stub">now content lands in U5.</p>
</RouteLayout>

<style>
  .stub { color: var(--ink-soft); font-size: var(--text-base); }
</style>
